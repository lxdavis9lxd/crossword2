<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Game Dashboard</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            padding: 0;
            margin: 0;
            background-color: #f5f5f5;
        }
        
        header {
            background-color: #4a6ea9;
            color: white;
            padding: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        nav ul {
            display: flex;
            list-style: none;
            padding: 0;
            margin: 0;
        }
        
        nav ul li {
            margin-left: 1.5rem;
        }
        
        nav ul li a {
            color: white;
            text-decoration: none;
        }
        
        nav ul li a:hover {
            text-decoration: underline;
        }
        
        main {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }
        
        .welcome-box {
            background-color: white;
            border-radius: 8px;
            padding: 1.5rem;
            margin-bottom: 2rem;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        
        .stats {
            display: flex;
            gap: 1rem;
            margin-top: 1rem;
        }
        
        .stat-box {
            background-color: #e8f0fe;
            border-radius: 8px;
            padding: 1rem;
            flex: 1;
            text-align: center;
        }
        
        .stat-box h3 {
            margin-top: 0;
            font-size: 16px;
            color: #555;
        }
        
        .stat-box p {
            font-size: 24px;
            font-weight: bold;
            margin: 0;
            color: #4a6ea9;
        }
        
        section {
            background-color: white;
            border-radius: 8px;
            padding: 1.5rem;
            margin-bottom: 2rem;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        
        h2 {
            margin-top: 0;
            color: #4a6ea9;
            border-bottom: 1px solid #eee;
            padding-bottom: 0.5rem;
        }
        
        form {
            display: flex;
            flex-direction: column;
            max-width: 400px;
        }
        
        label {
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            cursor: pointer;
        }
        
        input[type="radio"] {
            margin-right: 0.5rem;
        }
        
        button {
            background-color: #4a6ea9;
            color: white;
            border: none;
            padding: 0.75rem 1.5rem;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1rem;
            margin-top: 1rem;
            transition: background-color 0.3s;
        }
        
        button:hover {
            background-color: #3a5b8c;
        }
        
        #saved-games-list {
            list-style: none;
            padding: 0;
        }
        
        .saved-game-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem;
            border-bottom: 1px solid #eee;
        }
        
        .saved-game-item:last-child {
            border-bottom: none;
        }
        
        .empty-state {
            text-align: center;
            color: #888;
            padding: 2rem 0;
        }
    </style>
</head>
<body>
    <header>
        <h1>Crossword Game Dashboard</h1>
        <nav>
            <ul>
                <li><a href="/auth/logout">Logout</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <section class="welcome-box">
            <h2>Welcome, <%= user.username %>!</h2>
            <p>Ready to challenge your vocabulary and problem-solving skills?</p>
            
            <div class="stats">
                <div class="stat-box">
                    <h3>Games Played</h3>
                    <p><%= user.gamesPlayed %></p>
                </div>
                <div class="stat-box">
                    <h3>Puzzles Solved</h3>
                    <p><%= user.puzzlesSolved %></p>
                </div>
                <div class="stat-box">
                    <h3>Current Streak</h3>
                    <p><%= user.streak %></p>
                </div>
            </div>
        </section>
        
        <section>
            <h2>Select Difficulty Level</h2>
            <form action="/game/start" method="POST">
                <label>
                    <input type="radio" name="level" value="easy" checked required> Easy
                </label>
                <label>
                    <input type="radio" name="level" value="intermediate"> Intermediate
                </label>
                <label>
                    <input type="radio" name="level" value="advanced"> Advanced
                </label>
                <button type="submit">Start Game</button>
            </form>
        </section>
        
        <section>
            <h2>Saved Games</h2>
            <div id="saved-games-container">
                <% if (hasSavedGames) { %>
                    <ul id="saved-games-list">
                        <% savedGames.forEach(game => { %>
                            <li class="saved-game-item">
                                <div>
                                    <h3><%= game.level.charAt(0).toUpperCase() + game.level.slice(1) %> Level Puzzle</h3>
                                    <p>Last played: <%= game.lastUpdated %></p>
                                </div>
                                <form action="/game/resume" method="POST">
                                    <input type="hidden" name="puzzleId" value="<%= game.puzzleId %>">
                                    <input type="hidden" name="progress" value='<%= game.progress %>'>
                                    <button type="submit" class="resume-button">Resume Game</button>
                                </form>
                            </li>
                        <% }); %>
                    </ul>
                <% } else { %>
                    <div class="empty-state">
                        <p>You don't have any saved games yet.</p>
                    </div>
                <% } %>
            </div>
        </section>
    </main>
    
    <script>
        // Fetch saved games for this user
        document.addEventListener('DOMContentLoaded', function() {
            // This is a placeholder for when we implement saved games functionality
            // We would typically make an AJAX call to get the user's saved games
            const savedGamesContainer = document.getElementById('saved-games-container');
            const savedGamesList = document.getElementById('saved-games-list');
            
            // For now, just hide the empty state if we had saved games
            // In a real implementation, we would populate this list from the server
        });
    </script>
</body>
</html>
