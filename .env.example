# App Environment
NODE_ENV=production
PORT=8080

# Session Secret (change to a secure random string)
SESSION_SECRET=change_this_to_a_long_secure_random_string

# SQLite Configuration
# This path should be in a persistent location on Azure
SQLITE_PATH=/home/site/wwwroot/data/database.sqlite

# Session Store Configuration
# Option 1: Redis Cache (if you need distributed sessions)
# REDIS_URL=redis://username:password@your-azure-redis.redis.cache.windows.net:6380?tls=true

# Option 2: Azure Table Storage (if you need distributed sessions)
# AZURE_STORAGE_ACCOUNT=yourstorageaccount
# AZURE_STORAGE_ACCESS_KEY=youraccesskey
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=yourstorageaccount;AccountKey=youraccesskey;EndpointSuffix=core.windows.net

# Admin Configuration (used in migrations/scripts)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=strong_password_here
